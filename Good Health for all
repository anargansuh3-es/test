<!DOCTYPE html>
<html lang="mn">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GAMEHUB – Gaming Platform</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600;800&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
         :root {
            --bg-dark: #0b0f1a;
            --bg-card: #12182b;
            --blue: #00e5ff;
            --pink: #ff3cac;
            --purple: #784ba0;
            --text: #fff;
            --muted: #b8c1ec;
        }
        
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: radial-gradient(circle at top, #151c3b, var(--bg-dark));
            color: var(--text);
            min-height: 100vh;
            overflow-x: hidden;
        }
        /* HEADER */
        
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 22px 8%;
            position: relative;
            z-index: 100;
        }
        
        .logo {
            font-family: 'Orbitron', sans-serif;
            font-weight: 800;
            font-size: 1.5rem;
            color: var(--blue);
            letter-spacing: 2px;
            text-shadow: 0 0 15px rgba(0, 229, 255, 0.6);
        }
        
        nav a {
            margin-left: 28px;
            color: var(--muted);
            text-decoration: none;
            cursor: pointer;
            transition: color 0.3s;
            font-weight: 500;
        }
        
        nav a.active,
        nav a:hover {
            color: #fff;
        }
        /* LAYOUT */
        
        main {
            padding: 60px 8%;
        }
        
        .page {
            display: none;
            animation: fadeIn 0.4s;
        }
        
        .page.active {
            display: block;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* HERO */
        
        .hero {
            display: grid;
            grid-template-columns: 1.2fr 0.8fr;
            gap: 40px;
            align-items: center;
        }
        
        .hero h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 3rem;
            line-height: 1.2;
            background: linear-gradient(90deg, var(--blue), var(--pink), var(--purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 20px;
        }
        
        .hero p {
            color: var(--muted);
            margin: 20px 0 30px;
            font-size: 1.05rem;
        }
        
        .btn {
            padding: 14px 34px;
            border-radius: 30px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            border: none;
            transition: all 0.3s ease;
        }
        
        .btn.primary {
            background: linear-gradient(90deg, var(--blue), var(--pink));
            color: #000;
            box-shadow: 0 0 25px rgba(0, 229, 255, 0.5);
        }
        
        .btn.primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 40px rgba(255, 60, 172, 0.7);
        }
        
        .hero-card {
            background: linear-gradient(145deg, #141a33, var(--bg-card));
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 0 40px rgba(0, 0, 0, 0.6);
            position: relative;
            overflow: hidden;
        }
        
        .hero-card::before {
            content: "";
            position: absolute;
            inset: -2px;
            background: linear-gradient(120deg, var(--blue), var(--pink), var(--purple));
            filter: blur(20px);
            opacity: 0.5;
            z-index: 0;
        }
        
        .hero-card-content {
            position: relative;
            z-index: 1;
        }
        
        .hero-card h3 {
            font-family: 'Orbitron', sans-serif;
            color: var(--blue);
            margin-bottom: 10px;
            font-size: 1.5rem;
        }
        
        .hero-card p {
            color: var(--muted);
            margin: 0;
        }
        /* STATS */
        
        .stats {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-top: 60px;
        }
        
        .card {
            background: var(--bg-card);
            padding: 25px;
            border-radius: 20px;
            text-align: center;
            transition: transform 0.3s;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
        }
        
        .card:hover {
            transform: translateY(-5px);
        }
        
        .card h3 {
            color: var(--blue);
            font-family: 'Orbitron', sans-serif;
            font-size: 2rem;
            margin-bottom: 10px;
        }
        
        .card p {
            color: var(--muted);
            font-size: 0.9rem;
        }
        /* GAMES */
        
        .games {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }
        
        .game {
            background: var(--bg-card);
            border-radius: 20px;
            padding: 40px 20px;
            text-align: center;
            transition: all 0.3s;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: 600;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.4);
        }
        
        .game:hover {
            transform: translateY(-6px);
            background: linear-gradient(120deg, var(--blue), var(--pink));
            color: #000;
            box-shadow: 0 0 30px rgba(255, 60, 172, 0.6);
        }
        /* SECTION HEADERS */
        
        .page h2 {
            font-family: 'Orbitron', sans-serif;
            font-size: 2.5rem;
            margin-bottom: 20px;
            background: linear-gradient(90deg, var(--blue), var(--pink));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .page>p {
            color: var(--muted);
            font-size: 1.1rem;
            margin-bottom: 30px;
        }
        /* CHAT */
        
        .chat-container {
            display: grid;
            grid-template-columns: 250px 1fr 200px;
            height: 70vh;
            background: #0f162f;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 0 40px rgba(0, 0, 0, 0.6);
        }
        
        .chat-sidebar {
            background: #111836;
            padding: 20px;
            border-right: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .chat-sidebar h3 {
            font-family: 'Orbitron', sans-serif;
            color: var(--blue);
            margin-bottom: 15px;
            font-size: 1.1rem;
        }
        
        .chat-sidebar h4 {
            color: var(--muted);
            margin-top: 20px;
            margin-bottom: 10px;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .channel,
        .server {
            padding: 10px;
            margin: 5px 0;
            border-radius: 8px;
            cursor: pointer;
            color: var(--muted);
            transition: all 0.3s;
        }
        
        .channel:hover,
        .server:hover {
            background: rgba(0, 229, 255, 0.1);
            color: var(--blue);
        }
        
        .channel.active,
        .server.active {
            background: rgba(0, 229, 255, 0.2);
            color: var(--blue);
            font-weight: 600;
        }
        
        .chat-main {
            display: flex;
            flex-direction: column;
        }
        
        .chat-header {
            padding: 15px 20px;
            background: #141a33;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            font-weight: 600;
            font-family: 'Orbitron', sans-serif;
            color: var(--blue);
        }
        
        .messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
        }
        
        .messages p {
            margin: 10px 0;
            padding: 8px 12px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 8px;
            line-height: 1.5;
        }
        
        .messages b {
            color: var(--blue);
        }
        
        .chat-input {
            display: flex;
            padding: 15px;
            background: #141a33;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            gap: 10px;
        }
        
        .chat-input input {
            flex: 1;
            border: none;
            border-radius: 20px;
            padding: 12px 18px;
            background: rgba(255, 255, 255, 0.1);
            color: var(--text);
            font-family: 'Poppins', sans-serif;
            outline: none;
        }
        
        .chat-input input::placeholder {
            color: var(--muted);
        }
        
        .chat-input button {
            border: none;
            border-radius: 20px;
            padding: 12px 24px;
            background: linear-gradient(90deg, var(--blue), var(--pink));
            color: #000;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .chat-input button:hover {
            transform: scale(1.05);
            box-shadow: 0 0 20px rgba(0, 229, 255, 0.6);
        }
        
        .chat-users {
            background: #111836;
            padding: 20px;
            border-left: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .chat-users h4 {
            font-family: 'Orbitron', sans-serif;
            color: var(--blue);
            margin-bottom: 15px;
            font-size: 1rem;
        }
        
        .user {
            padding: 8px;
            margin: 5px 0;
            border-radius: 8px;
            color: var(--muted);
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .user:hover {
            background: rgba(0, 229, 255, 0.1);
            color: var(--blue);
        }
        /* RESPONSIVE */
        
        @media(max-width: 900px) {
            .hero {
                grid-template-columns: 1fr;
                text-align: center;
            }
            .stats {
                grid-template-columns: 1fr 1fr;
            }
            .chat-container {
                grid-template-columns: 1fr;
                height: auto;
            }
            .chat-sidebar,
            .chat-users {
                display: none;
            }
            nav {
                display: flex;
                flex-wrap: wrap;
            }
            nav a {
                margin-left: 15px;
                font-size: 0.9rem;
            }
        }
        /* SCROLLBAR */
        
         ::-webkit-scrollbar {
            width: 8px;
        }
        
         ::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.05);
        }
        
         ::-webkit-scrollbar-thumb {
            background: var(--blue);
            border-radius: 4px;
        }
        
         ::-webkit-scrollbar-thumb:hover {
            background: var(--pink);
        }
    </style>
</head>

<body>

    <header>
        <div class="logo">GAMEHUB</div>
        <nav id="mainNav">
            <a onclick="openPage('home')" class="nav-link active">Нүүр</a>
            <a onclick="openPage('games')" class="nav-link">Тоглоом</a>
            <a onclick="openPage('community')" class="nav-link">Коммюнити</a>
            <a onclick="openPage('tournaments')" class="nav-link">Тэмцээн</a>
            <a onclick="openPage('chat')" class="nav-link">Чат</a>
        </nav>
    </header>

    <main>

        <!-- HOME PAGE -->
        <section id="home" class="page active">
            <div class="hero">
                <div>
                    <h1>Next-Level<br>Gaming Hub</h1>
                    <p>Тоглоом, чат, тэмцээн — бүгд нэг дор. Дэлхийн шилдэг тоглогчидтой холбогдоод өөрийн ур чадвараа сорьцгооё!</p>
                    <button class="btn primary" onclick="openPage('games')">Тоглож эхлэх</button>
                </div>
                <div class="hero-card">
                    <div class="hero-card-content">
                        <h3>🔴 LIVE</h3>
                        <p>Valorant Tournament<br>Өнөөдөр 21:00</p>
                    </div>
                </div>
            </div>

            <div class="stats">
                <div class="card">
                    <h3>128</h3>
                    <p>🟢 Online тоглогч</p>
                </div>
                <div class="card">
                    <h3>12</h3>
                    <p>🎮 Тоглоом</p>
                </div>
                <div class="card">
                    <h3>4</h3>
                    <p>💬 Чат суваг</p>
                </div>
                <div class="card">
                    <h3>1</h3>
                    <p>🔴 Live тэмцээн</p>
                </div>
            </div>
        </section>

        <!-- GAMES PAGE -->
        <section id="games" class="page">
            <h2>🎮 Game Library</h2>
            <p>Өөрийн дуртай тоглоомоо сонгоод хамт тогло</p>
            <div class="games">
                <div class="game">🔥 Valorant</div>
                <div class="game">⚔️ CS2</div>
                <div class="game">🕹️ Dota 2</div>
                <div class="game">🚀 Fortnite</div>
                <div class="game">🧙 League of Legends</div>
                <div class="game">🧟 PUBG</div>
                <div class="game">👾 Apex Legends</div>
                <div class="game">✨ Genshin Impact</div>
            </div>
        </section>

        <!-- COMMUNITY PAGE -->
        <section id="community" class="page">
            <h2>🧑‍🤝‍🧑 Community</h2>
            <p>Нэг нэгнээ олж, хамт тогло. Найзууд олоод команд болцгооё!</p>
            <div class="stats">
                <div class="card">
                    <h3>2,453</h3>
                    <p>Нийт гишүүн</p>
                </div>
                <div class="card">
                    <h3>128</h3>
                    <p>Онлайн одоо</p>
                </div>
                <div class="card">
                    <h3>15</h3>
                    <p>Команд</p>
                </div>
                <div class="card">
                    <h3>42</h3>
                    <p>Өнөөдөр нэгдсэн</p>
                </div>
            </div>
        </section>

        <!-- TOURNAMENTS PAGE -->
        <section id="tournaments" class="page">
            <h2>🏆 Tournaments</h2>
            <p>Шагналтай тэмцээнүүдэд оролцоод чамайгаа баталгаажуул</p>
            <div class="games">
                <div class="game">
                    <div style="font-size: 2rem; margin-bottom: 10px;">🔴</div>
                    <div>Valorant Championship</div>
                    <div style="font-size: 0.85rem; color: var(--muted); margin-top: 8px;">Өнөөдөр 21:00</div>
                </div>
                <div class="game">
                    <div style="font-size: 2rem; margin-bottom: 10px;">⏰</div>
                    <div>CS2 Weekly Cup</div>
                    <div style="font-size: 0.85rem; color: var(--muted); margin-top: 8px;">Маргааш 19:00</div>
                </div>
                <div class="game">
                    <div style="font-size: 2rem; margin-bottom: 10px;">🏅</div>
                    <div>Dota 2 Marathon</div>
                    <div style="font-size: 0.85rem; color: var(--muted); margin-top: 8px;">Бямба 15:00</div>
                </div>
                <div class="game">
                    <div style="font-size: 2rem; margin-bottom: 10px;">💰</div>
                    <div>Grand Finals</div>
                    <div style="font-size: 0.85rem; color: var(--muted); margin-top: 8px;">Ням 20:00</div>
                </div>
            </div>
        </section>

        <!-- CHAT PAGE -->
        <section id="chat" class="page">
            <h2>💬 Live Chat</h2>
            <div class="chat-container">
                <div class="chat-sidebar">
                    <h3>🎮 Servers</h3>
                    <div class="server active">GAMEHUB</div>

                    <h4>Channels</h4>
                    <div class="channel active"># general</div>
                    <div class="channel"># matchmaking</div>
                    <div class="channel"># tournaments</div>
                    <div class="channel"># voice</div>
                </div>

                <div class="chat-main">
                    <div class="chat-header"># general</div>
                    <div class="messages" id="messageBox">
                        <p><b>System:</b> GAMEHUB-д тавтай морил 🔥</p>
                        <p><b>Anar:</b> Valorant ranked хүн байна уу?</p>
                        <p><b>Bold:</b> Би явж чадна, даваа 3 баг хэрэгтэй</p>
                        <p><b>Nova:</b> +1 намайг нэмээрэй</p>
                    </div>
                    <div class="chat-input">
                        <input type="text" id="chatInput" placeholder="Message #general...">
                        <button id="sendBtn">Send</button>
                    </div>
                </div>

                <div class="chat-users">
                    <h4>Online — 12</h4>
                    <div class="user">🟢 Player01</div>
                    <div class="user">🟢 Player02</div>
                    <div class="user">🟢 Anar</div>
                    <div class="user">🟢 Bold</div>
                    <div class="user">🟢 Nova</div>
                    <div class="user">🟡 Player03</div>
                    <div class="user">🟡 Shadow</div>
                    <div class="user">🔴 AFK_User</div>
                </div>
            </div>
        </section>

    </main>

    <script>
        // PAGE NAVIGATION
        function openPage(pageId) {
            // Remove active from all pages and nav links
            document.querySelectorAll('.page').forEach(page => {
                page.classList.remove('active');
            });
            document.querySelectorAll('.nav-link').forEach(link => {
                link.classList.remove('active');
            });

            // Add active to selected page
            const selectedPage = document.getElementById(pageId);
            if (selectedPage) {
                selectedPage.classList.add('active');
            }

            // Add active to corresponding nav link
            event.target.classList.add('active');
        }

        // CHAT FUNCTIONALITY
        const chatInput = document.getElementById('chatInput');
        const sendBtn = document.getElementById('sendBtn');
        const messageBox = document.getElementById('messageBox');

        function sendMessage() {
            const message = chatInput.value.trim();
            if (message === '') return;

            // Create new message element
            const newMessage = document.createElement('p');
            newMessage.innerHTML = `<b>You:</b> ${message}`;
            messageBox.appendChild(newMessage);

            // Clear input
            chatInput.value = '';

            // Scroll to bottom
            messageBox.scrollTop = messageBox.scrollHeight;

            // Simulate response after 1 second
            setTimeout(() => {
                const responses = [
                    'Сайн байна уу! Би ч бас тоглохыг хүсч байна',
                    'Хэдэн цагт эхлэх вэ?',
                    'Миний rank Gold 2 байна',
                    'Discord дээр холбогдъё'
                ];
                const randomResponse = responses[Math.floor(Math.random() * responses.length)];
                const botMessage = document.createElement('p');
                botMessage.innerHTML = `<b>Bot:</b> ${randomResponse}`;
                messageBox.appendChild(botMessage);
                messageBox.scrollTop = messageBox.scrollHeight;
            }, 1000);
        }

        // Send message on button click
        sendBtn.addEventListener('click', sendMessage);

        // Send message on Enter key
        chatInput.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                sendMessage();
            }
        });

        // Channel switching
        document.querySelectorAll('.channel').forEach(channel => {
            channel.addEventListener('click', function() {
                document.querySelectorAll('.channel').forEach(ch => ch.classList.remove('active'));
                this.classList.add('active');
                document.querySelector('.chat-header').textContent = this.textContent;
            });
        });
    </script>

</body>

</html
